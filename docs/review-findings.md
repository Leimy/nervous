# Adversarial Review Findings

This ledger persists review concerns across status rewrites. A finding is closed only by a fix with evidence or a written argument showing it is not a defect. Deferral requires a destination milestone and owner. Review gates apply the severity rules in `COORDINATION.md`.

Findings from a gate whose milestone state is permanently `complete` are relocated to `review-findings-archive.md` to keep this file short; a section left here as a short pointer (see R1 below) means the gate is closed for good. A gate still accumulating pre-formal-review findings (see R2) stays here in full even after every listed finding is closed, because it is still open to new entries.

States:

```text
open
investigating
fixed-pending-verification
closed
deferred
```

## R1 - Foundation consolidation

Gate complete; milestone `R1` state is `complete` in `STATUS.md`. All 14 findings (R1-F01 through R1-F14, severities low through high, covering dispatch duplication, compiler/pattern ownership, aggregate test integration, verifier overflow, and documentation drift) are closed and will not reopen. The full table is relocated to `review-findings-archive.md` to keep this ledger short; read it only if investigating a regression that might touch that closed work.

## R2 - Concurrency and lifecycle

| ID | Severity | State | Owner | Finding | Required evidence |
|---|---|---|---|---|---|
| R2-F01 | high | closed | M05/R2 coordinator | Reusing a process slot after its `ulong` generation wraps could make an ancient stale PID alias a new process, violating D037 | Exhausted generations retire their slot, lookup accepts only live states, and the focused regression plus aggregate suite pass |
| R2-F02 | medium | closed | M05/R2 coordinator | A process-table growth during `spawn` could occur after the scheduler cursor wrapped against the old table, giving the parent another quantum before its new child | Scheduler reacquires the parent slot and computes the cursor after execution; focused post-spawn fairness regression and aggregate suite pass |
| R2-F03 | high | closed | M05/R2 coordinator | Receive wait/take/advance initially could not distinguish an exhausted active scan from no scan, allowing malformed bytecode to violate mailbox cursor lifecycle | Explicit scan-phase state guards begin/next/take/wait and administrative pop; focused block/rescan/preserve/take tests and the aggregate suite pass |
| R2-F04 | high | closed | M05 coordinator | Function and `match` clause patch loops rewrote pattern-test targets emitted by clause bodies, causing wrong fallthrough and preventing clause-form receive from blocking | Patch windows now end immediately after the outer pattern; aggregate suite executes the nested asserted-match failure regression and preserves `match_fail` |
| R2-F05 | high | closed | M05 coordinator | A message appended after scan exhaustion but before `recvwait` could leave a process sleeping with a matching queued message | `nvprocrecvwait` detects post-exhaustion append and yields runnable for oldest-first rescan; aggregate quantum-one regression consumes the appended message without sleeping |
| R2-F06 | medium | closed | M05 coordinator | No frame-depth limit existed and D039 overstated milestone 05 aggregate-memory accounting, so non-tail recursion could exhaust host memory | D047 adds configured per-process frame depth and `system_limit`; D039 assigns exact heap accounting to milestone 07; aggregate suite verifies exhaustion faults only the bounded execution |
| R2-F07 | low | closed | M05 coordinator | Compact semantics described every runtime fault as a term although early VM faults are bounded diagnostic strings | Semantics now distinguishes D029 symbolic VM diagnostics from arbitrary-term explicit exit and assigns first-class fault terms before links/monitors/catch |
| R2-F08 | medium | closed | M05 coordinator | Recursive frontend parsing and AST walks had no source-depth bound, allowing pathological input to exhaust the host C stack | Parser enforces `NvMaxsourcedepth` 256 before recursive descent; generated deep-source regression and complete aggregate suite pass. Scope was incomplete for iteratively built operator chains; that residue is tracked and fixed as R2-F14 |
| R2-F09 | low | closed | M05 coordinator | Runtime term-depth exhaustion was collapsed into the misleading `out_of_memory` reason | Value construction returns distinct `NvValuelimit`; `Otuple` maps it to `system_limit`; API, execution, and aggregate regressions pass |
| R2-F10 | low | closed | M05 coordinator | `nvschedspawn` could inspect an uninitialized or stale caller error buffer on its limit-setting failure path | `nvschedspawn` clears a supplied error buffer on entry; complete aggregate suite passes |
| R2-F11 | evidence | closed | M05 coordinator | The clause-patch closure lacked executable coverage of a clause-form receive actually blocking | Clause-form CLI fixture blocks and produces the exact aggregate deadlock diagnostic rather than `function_clause` |
| R2-F12 | evidence | closed | M05 coordinator | Receive behavior was pinned only at quantum one rather than checked for representative quantum independence | Aggregate scheduler test passes the same selective-receive outcome at quanta 1 through 4 |
| R2-F13 | evidence | closed | M01/M05 coordinator | D021 formatter idempotence was asserted but only one formatting pass was tested | Aggregate frontend harness reformats every canonical golden output and requires byte identity |
| R2-F14 | medium | closed | M05/R2 coordinator | R2-F08 bounded only recursive descent. `parseexpr1` builds left-associative operator chains iteratively, so `1+1+...+1` kept parser depth near zero while producing an unbounded AST left spine that `exprfree`, `fmtexpr`, `printexpr`, and `compilebinary` still walk recursively; a large chain exhausted the host C stack during teardown, formatting, AST printing, or compilation | `parseexpr1` charges every chain link against `NvMaxsourcedepth`, so AST depth is bounded whether it was built by recursion or iteration; `tests/pattern/parsetest.c` accepts a 64-link chain and rejects a 256-link chain with `nesting too deep`; `mk tests` builds and the complete aggregate `rc tests/run.rc` passes, including every frontend golden and formatter-idempotence pass that the changed parser path feeds |

### R2 formal gate

**Closed.** The formal R2 review (STATUS.md task R2-T01) ran four parallel read-only investigators against `milestones/R2-concurrency-review.md`'s adversarial questions and required work: lifecycle/state, mailbox/messages, timers/deadlines, and fairness/limits. Each investigator's full report (function-level citations for every claim, including "no finding") lived at a `/tmp/r2-<lens>.md` scratch path during the review; that path is not durable and is not cited as evidence below -- every claim this ledger relies on is repeated here with its own citation into the current source, independently re-verified by the coordinator before entry (the same discipline R2-F09 through R2-F13's closure already used). The user ran `mk tests` and `rc tests/run.rc` and confirmed a complete pass, including every regression named below (`tests/process/r2test.c`'s five `ok - R2...` lines and four `ok - tiny: ...` lines, plus the unchanged existing aggregate suite); every `evidence`/`medium` finding below moved from `fixed-pending-verification` to `closed` on that confirmation. R2-F16 remains `deferred` (see its row) -- that is its correct final state, not an open item.

| ID | Severity | State | Owner | Finding | Required evidence |
|---|---|---|---|---|---|
| R2-F15 | medium | closed | R2 coordinator | A process that satisfies a timed receive's `after` clause by matching a message on its very first scan never calls `nvprocrecvwait`/`nvprocrecvwaitdeadline`, so `hasdeadline`/`deadline` stay armed, never consumed, all the way to process exit. Neither `nvprocexit` nor `nvprocspawn`'s slot-reuse branch cleared those fields (only the grow-new-slot `memset` did), so a later process reusing that slot inherited a stale armed deadline. This is the milestone-06 gap deferred to R2 (`STATUS.md`, `milestones/06-timeouts.md`): its inspection argument ("D037/D041 already prevent a reused slot from inheriting a stale wait state") was half right and half wrong -- `nvprocexit` does free the mailbox/exec unconditionally as claimed, but D037 (PID aliasing) and D041 (state-transition rules) do not zero this field; an incidental guard did the actual work instead. The defect was latent, not exploitable: `nvschedstep`'s idle deadline scan gates every check on `p->state == Prwaiting`, and a freshly spawned or freshly exited slot is never `Prwaiting` before its own code (re)arms a real deadline. But D050's stated mechanism ("a deadline cannot outlive its slot") was true only of the field's one reader, not of the field itself -- a future reader that does not first check state (a diagnostic, multicore prework, or a bug) would have observed and acted on the stale value | Fixed: `nvprocexit` (`lib/process.c`) now clears `hasdeadline`/`deadline` alongside its existing `execfree`/`mailboxfree`, and `nvprocspawn`'s reuse branch clears them again for defense in depth. `tests/process/r2test.c`'s `h1regression` constructs the exact scenario (timed receive matches immediately, deadline never consumed, process exits, slot is reused) and asserts both fields are zero on the dead slot and on the slot immediately after reuse. User-confirmed passing (`ok - R2-F15: slot reuse and exit both clear a stale armed deadline`), `rc tests/run.rc`, full suite green |
| R2-F16 | low | deferred to milestone 10 | R2 coordinator | `NvLimits.maxduration` (`include/nvproc.h`) is declared as if it were a per-runtime configurable ceiling, matching the other five `NvLimits` fields, but it is dead: `nvruntimeinit` never validates it and `nvprocarmdeadline` checks the compile-time `NvMaxduration` constant (D049) unconditionally, never `r->limits.maxduration`. Two of the three existing runtime-limit initializers (`tests/process/ptest.c`, `tests/process/schedtest.c`) never even set the field, which is harmless only because nothing reads it | Not fixed this session: enforcing it is a real semantic decision (a runtime could then legitimately accept a narrower duration domain than D049's literal), not a drive-by fix inside a review gate. Deferred to milestone 10 (`docs/questions.md`'s "Milestone 10 - Multicore" section owns "Timer ownership across schedulers," the natural home for deciding this). `include/nvproc.h` now documents the field as advisory-only at its declaration so it cannot mislead a future reader in the meantime |
| R2-F17 | evidence | closed | R2 coordinator | The mailbox lens found no defect in selective-receive ordering, but no existing test exercised a message appended to the tail while the scan cursor was sitting on an earlier, not-yet-exhausted candidate (as opposed to R2-F05's already-covered post-exhaustion race) | `tests/process/r2test.c`'s `midscanappend` (quantum 1, pre-queued non-matching candidate, inject a matching message while `scanning==1 && scan != nil`) asserts the injected message is found by the same scan's `recvnext`, not lost or misrouted. User-confirmed passing, `rc tests/run.rc`, full suite green |
| R2-F18 | evidence | closed | R2 coordinator | The mailbox lens found no aliasing defect in copied message graphs (every `NvValue` kind traced through `nvvaluecopy`/`valuecopydepth`), but no existing test drove a sender through send-then-immediate-return (ordinary completion, not `exit()`) followed by full reaping, then verified the receiver's copy is still intact | `tests/process/r2test.c`'s `sendthenexit` spawns a receiver as root, a sender that sends a tuple and returns, confirms `nvprocalive` is false for the reaped sender, then asserts the receiver's returned value's content is byte-correct. User-confirmed passing, `rc tests/run.rc`, full suite green |
| R2-F19 | evidence | closed | R2 coordinator | The lifecycle and timers lenses independently found that `nvprocrecvtake` does not clear `hasdeadline`, so a timed receive that matches leaves it stale (see R2-F15); no existing test confirmed that a later plain receive in the same process correctly clears it (via `nvprocrecvwait`'s existing, commented clear) before blocking, rather than leaking it into the idle deadline scan | `tests/process/r2test.c`'s `crossreceiveleak`: a timed-then-plain receive fixture matches the timed clause immediately (deadline never consumed), enters a nested plain receive, and asserts `hasdeadline == 0` once it blocks, and that an idle step reports plain deadlock (`NvSchedIdle`) rather than a phantom timer wakeup. User-confirmed passing, `rc tests/run.rc`, full suite green |
| R2-F20 | evidence | closed | R2 coordinator | The fairness lens found the round-robin sweep is structurally total (every slot visited once per sweep; no starvation is reachable), but flagged that `schedtest.c`'s "spawned child is next in round-robin order" assertion is only incidentally true (child always lands at `parent_slot+1` in that fixture) and does not exercise a child landing *below* the current cursor after an earlier, lower-slotted process exits | `tests/process/r2test.c`'s `belowcursorfairness` spawns three processes, exits the lowest-slotted one, spawns a fourth that reuses that now-lower slot (confirmed below the cursor), and asserts all three remaining processes are dispatched at least once within one full sweep. User-confirmed passing, `rc tests/run.rc`, full suite green |

### Post-gate defects (found while writing example programs)

Not part of the formal R2 review, but recorded in this ledger because both touch R2's surface (frame limits, root lifecycle reporting) and were found by the first real program to exercise them at scale: `examples/sieve.nv` with `generate(2, 1024, 'nil)`. Both rows move to `closed` once `rc tests/run.rc` is confirmed passing by the user (the fixing agent could compile, not execute).

| ID | Severity | State | Owner | Finding | Required evidence |
|---|---|---|---|---|---|
| R2-F21 | medium | fixed-pending-verification | user (defect fix, post-R2) | D047 states "Tail calls retain their current depth" and the VM implements `Otailcall` (frame replacement, no `maxframe` check), but `lib/compile.c` never emitted it: every `Ecall` lowered to `Ocall`. So every self-recursive loop -- including the message-loop idiom `fn loop(s) { receive { m => loop(s2); } }` that the whole process model is built around -- grew one frame per iteration and faulted `system_limit` at `maxframe` (1024 in the CLI). `sieve.nv` was tuned to a limit of 1023 exactly one frame under that cliff; 1024 faulted the root | Fixed: `compileexpr` now carries a tail flag (clause body; last expression of a tail block; every arm of a tail `if`/`match`/`receive` including the `after` body) and emits `tailcall` there, returning a sentinel so the enclosing construct omits the unreachable `move`/`jump`/`return`. `docs/semantics.md` "Functions" defines tail position normatively. `tests/pattern/compiletest.c`: a 3000-deep tail loop succeeds under the default `maxframe` 1024 (via `if`, and via `match` + nested block), its non-tail twin still faults `system_limit`, and the lowering contains `tailcall` and no `call`. `tests/run.rc` runs `examples/sieve.nv` and requires 173 output lines. `mk tests` builds clean; `rc tests/run.rc` not yet run |
| R2-F22 | low | fixed-pending-verification | user (defect fix, post-R2) | D046 says a root fault or explicit exit "is reported as failure", but `cmd/nervous/main.c` tested `state == NvSchedIdle` before `sched.rootstate`. When the root faulted and the surviving processes then blocked forever waiting on it (the D046 "their disappearance leaves ... another live process permanently blocked" case, in reverse), the run ended idle and printed only `deadlock: N live process(es), none runnable`, swallowing the `fault system_limit` that caused it. The symptom was maximally misleading: all output looked complete, then "deadlock" | Fixed: root fault/exit is reported first; if the scheduler also ended idle, a second line `deadlock: N live process(es) orphaned by the root, none runnable` follows. A genuine deadlock (root alive or done normally) prints the unchanged single line, so `tests/process/cli/deadlock.err` is untouched. New fixture `tests/process/cli/orphan.nv`/`orphan.err` (root faults `match_fail` with a child blocked in receive) is in `run.rc`'s CLI diagnostic loop. `mk tests` builds clean; `rc tests/run.rc` not yet run |

Not implemented this session, named here rather than only in a volatile scratch file so the gap is durable: the fairness investigator's fuller "tiny limits" design (T5-03 message-exactly-at-`maxmessage`, T5-06 tiny `maxduration` -- blocked on R2-F16, T5-07 full-aggregate-suite-under-tiny-limits) and fuller "required work item 2" design (a dedicated spawn-storm reduction-floor test). `tests/process/r2test.c`'s `tinylimits` covers the `maxprocess`, mailbox-byte, `maxframe`, and per-runtime `maxtermdepth` boundaries; `belowcursorfairness` covers the round-robin/slot-reuse half of item 2. The state-transition invariants required by "Required work" item 1 (model every legal/illegal state combination) are documented in the lifecycle investigator's analysis, distilled here: `NvProcess.state` is a plain `int` (not a bitmask) written only by guarded, mutually-exclusive transition functions; `scanning` is 1 if and only if `state == Prrunning`; `hasdeadline` is not state-guarded by construction but is now cleared at every slot-death/reuse point (R2-F15) and is otherwise read only where `state == Prwaiting` is also checked, so a stale value cannot cause a spurious wakeup. See `milestones/R2-concurrency-review.md`'s closing note for the exit-criterion status.

## R3 - Memory and representation

| ID | Severity | State | Owner | Finding | Required evidence |
|---|---|---|---|---|---|
| R3-F01 | medium | closed | M05/R3 coordinator | Recursive size/copy/free/equality over program-controlled tuple depth could exhaust the host C stack before controlled resource reporting | D047 caps language-created terms at depth 256 and allows a smaller runtime send ceiling; aggregate construction-boundary regression passes; arbitrary malformed host graphs and final iterative representation remain milestone 07 hardening |

## R4 - Multicore

No findings recorded yet. Populate during R4 rather than relying on memory.
